meta {
  name: applyCommands - stale head (409)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v2/sync/applyCommands?head={{rootId}}
  body: json
  auth: none
}

params:query {
  head: {{rootId}}
}

docs {
  Applying against a head that is no longer the tip must 409.
  This uses the root command id as `head`; it only produces a 409 once at least
  one command has been appended past root (e.g. after running any Item/Container
  create scenario). On a truly fresh DB where root == head this succeeds instead.
}

script:pre-request {
  function uuid() {
    return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
      const r = Math.random() * 16 | 0;
      const v = c === 'x' ? r : (r & 0x3 | 0x8);
      return v.toString(16);
    });
  }
  bru.setVar('staleCmdId', uuid());
  bru.setVar('staleContainerId', uuid());
}

body:json {
  [
    {
      "parent": "{{rootId}}",
      "child": null,
      "command": {
        "id": "{{staleCmdId}}",
        "command_type": "CONTAINER_CREATE",
        "command_version": 1,
        "payload": {
          "id": "{{staleContainerId}}",
          "name": "Stale Head Container",
          "parent": "{{rootId}}",
          "created_at": "2000-01-01T00:00:00"
        }
      }
    }
  ]
}

tests {
  test("status 409 (once chain has moved past root)", function () {
    expect(res.getStatus()).to.equal(409);
  });
}